<h1>Articles</h1>

<ul>
  <% @articles.each do |article| %>
    <li>
      <%= link_to article.title, article %>
      <!-- the line above replaces the two lines below -->
<!--      <a href="<%#= article_path(article) %>">-->
        <%#= article.title %>
      </a>
    </li>
  <% end %>
</ul>

<%= link_to "New Article", new_article_path %>
